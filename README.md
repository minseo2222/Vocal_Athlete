# vocal_athlete

This repository contains a Flutter app under `app/`.

Run app verification commands from `app/`:

```sh
flutter pub get
flutter analyze
flutter test
```

Repository-level docs live in `CONTEXT.md`, `CONTEXT-MAP.md`, `docs/adr/`, and
`docs/verification/`. Project-specific agent instructions are in `AGENTS.md`.
